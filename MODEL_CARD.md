# Rescue79 hard4 정적 7·9 모델 카드

- 모델 이름: Rescue79 hard4 portable v1
- 공개 파일: `models/rescue79-hard4-portable-v1.pt`
- 공개일: 2026-08-12
- 모델 상태: 생성형 정지영상 진단용 연구 모델
- 운영 상태: 실제 CCTV 현장 성능 미입증
- 라이선스: 프로젝트 소유자가 해당 hard4 가중치를 MIT로 공개
- 공개 모델 SHA-256:
  `603cf711a4ccd59119c63a207bb78f3399ce8860c6eed5d6692481f13ff2db0a`

## 한눈에 보는 모델 범위

이 모델은 RGB 정지사진에서 별도 자세 인식기가 추출한 COCO-17 관절을 이용해 숫자
7과 숫자 9 자세를 비교·검토하는 hard4 어댑터입니다.

다음 기능을 제공하지 않습니다.

- 실제 구조 요청 또는 조난 의도 판정
- 완전한 `7→9→7→9` 시간 순서 검출
- 실시간 CCTV 관제
- 자동 112·119 신고
- 자동 출동 또는 위험도 결정

사람이 정답을 알고 있는 시험사진에서 모델 결과와 원본·관절 오버레이를 비교하기
위한 도구입니다.

## 의도한 사용

- 생성형·합성·동의된 정지 시험사진에서 7·9 자세 판별 품질 확인
- 모델 버전 간 결과 비교
- COCO-17 관절 추출과 분류 결과의 시각적 점검
- 교육, 연구 시연과 오류 사례 수집
- 실제 연속영상 연구 전의 제한된 정적 진단

## 의도하지 않은 사용

- 응급상황 또는 구조 필요 여부 결정
- 사람의 의도, 건강상태, 범죄 또는 위험상태 추론
- 실제 CCTV 자동 경보
- 자동 신고·자동 출동
- 법적 증거 또는 공인 안전 인증
- 사람에 대한 제재·차별·권리 제한 결정
- 실제 CCTV 정확도 100% 주장
- 정지사진에서 시간 순서가 있었다는 주장

## 7·9 실험 자세 정의

이 프로젝트의 7·9 자세는 사용자 정의 실험안이며 공식 국가표준, 군 공식 교범,
공인 긴급 구조 수신호 또는 관계기관 승인 프로토콜이 아닙니다.

- `DIGIT_7`: 한 팔은 몸의 옆 방향으로 펴고 다른 팔은 자연스럽게 이완
- `DIGIT_9`: 한 손은 머리 영역 가까이에 두고 다른 팔은 자연스럽게 이완
- 좌우 정책: 어느 팔을 사용해도 같은 숫자로 취급
- 정적 검토 범위: 한 장의 토큰 자세만 비교

머리 긁기, 전화받기, 모자 만지기 등은 `DIGIT_9`와 관절 기하가 비슷할 수
있습니다. COCO-17 관절만으로 사람의 실제 의도를 구분할 수 없습니다.

## 처리 흐름

```text
RGB 정지사진
  → Torchvision Keypoint R-CNN으로 사람·COCO-17 관절 추출
  → 한 사람·관절 품질 검사
  → 정지 관절을 48프레임 유지 입력으로 변환
  → hard4 TCN 어댑터 추론
  → Top-1·Top-3·비보정 점수
  → 원본·관절 오버레이와 함께 사람이 검토
```

RGB 자세 인식기는 hard4 체크포인트에 포함되지 않습니다. 런타임에서 별도로
`KeypointRCNN_ResNet50_FPN_Weights.COCO_V1`을 사용합니다.

## 모델 계보

hard4는 기존 포병 숫자 자세 TCN v2.1을 기반으로 만든 생성형 정지영상 도메인
어댑터입니다.

기반 모델의 주요 특성:

- 입력: 48프레임 × COCO-17 관절 × 6특징
- 출력 클래스: `DIGIT_0..9`, `NEUTRAL`, `UNKNOWN`
- 기반 학습자료: 절차적으로 생성한 합성 관절 시퀀스 15,840개
- 시간 구조: TCN

hard4 적응 학습에서 변경한 부분:

- `DIGIT_7` classifier weight 행과 bias
- `DIGIT_9` classifier weight 행과 bias

동결한 부분:

- TCN backbone
- 다른 숫자 출력 행
- `NEUTRAL`과 `UNKNOWN` 출력 행

따라서 hard4는 전체 모델을 실제 CCTV로 다시 학습한 모델이 아닙니다. 기존 합성
관절 모델의 7·9 출력 부분을 생성형 정지사진 관절에 맞게 제한적으로 보정했습니다.

## 적응 학습자료

원본 생성형 정지사진은 총 34장입니다.

| 항목 | 수량 |
| --- | ---: |
| `DIGIT_7` | 16장 |
| `DIGIT_9` | 18장 |
| 전체 | 34장 |
| 이전 평가에서 수집한 어려운 사례 | 4장 |

각 사진에 32개의 변형을 적용해 학습 입력을 만들었습니다. 34 × 32 = 1,088은 서로
독립적으로 촬영된 원본 1,088장이 아니라 34장에 변형을 적용한 입력 수입니다.

변형 범위:

- 작은 회전: 약 -8도에서 +8도
- 작은 크기 변화
- 작은 위치 이동
- 작은 관절 좌표 흔들림
- COCO 좌우 관절을 맞바꾼 수평 반전
- 같은 정적 자세를 48프레임 동안 유지한 입력

학습 설정:

- epoch: 80
- batch size: 64
- learning rate: 0.001
- 이미지당 변형: 32
- 학습 라벨: `DIGIT_7`, `DIGIT_9`

학습자료의 라벨은 생성 의도와 manifest에 기초한 실험 라벨입니다. 독립적인 실제
사람 전문가가 실제 구조 의도를 판정한 ground truth가 아닙니다.

## 어려운 사례 보강

기존 모델이 틀린 생성형 정지사진 4장을 어려운 사례로 추가했습니다. 이는 모델이
실패한 형상을 보강하는 hard-example 학습입니다.

이 4장은 추가된 뒤 학습자료가 됐으므로 더 이상 독립 holdout 성능 근거로 사용할
수 없습니다. 최종 평가는 별도로 고정한 생성형 50장 세트를 사용했습니다.

## 평가자료와 결과

사전 고정한 생성형 CCTV 스타일 정지사진 50장을 사용했습니다.

| 항목 | 결과 |
| --- | ---: |
| 전체 사례 | 50 |
| `DIGIT_7` | 25/25 |
| `DIGIT_9` | 25/25 |
| 전체 채점 | 50/50 |
| 판정 보류 | 0 |
| 처리 오류 | 0 |

평가자료는 생성 후 고정했고, 해당 시험에서 재생성하거나 틀린 사진을 골라내지
않았습니다. 같은 50장을 반복 실행한 결과가 같았지만, 반복 실행은 새로운 독립
평가자료가 아닙니다. 따라서 세 번 실행했다고 150장 독립시험으로 표현하지 않습니다.

## 50/50이 의미하는 것

의미하는 것:

- 해당 사전 고정 생성형 정지사진 50장에서 7과 9를 모두 구분함
- 해당 실행에서 50장 모두 한 사람·관절 품질 조건을 통과함
- 같은 모델과 같은 입력의 반복 실행 결과가 재현됨

의미하지 않는 것:

- 실제 CCTV 정확도 100%
- 실제 조난자 검출률 100%
- 오경보율 0%
- 실제 사람·카메라·환경 일반화 완료
- 완전한 `7979` 시간 순서 검출 완료
- 실시간 처리 완료

## 평가의 중요한 결손

50장에는 명확한 `DIGIT_7`과 `DIGIT_9` 양성 사례만 있습니다. 다음 음성 사례가
포함되지 않았습니다.

- 전화받기
- 머리 긁기와 얼굴 만지기
- 손 흔들기와 스트레칭
- 걷기와 팔 흔들림
- 넘어지며 바닥 짚기
- 평범한 앉기·눕기
- `NEUTRAL`과 `UNKNOWN`

따라서 hard4의 다음 수치는 현재 계산할 수 없습니다.

- 특이도
- 정밀도
- 실제 오경보율
- 정상 행동이 7·9로 잘못 분류되는 비율
- CCTV 한 시간당 거짓 후보 수

## 점수 보정 상태

화면의 softmax 점수는 보정되지 않은 순위 점수입니다. 점수 `0.99`를 정답일 확률,
구조 요청일 확률 또는 신고 필요 확률 99%로 해석하면 안 됩니다.

별도의 실제 데이터 calibration과 신뢰도 평가가 완료되지 않았습니다.

## 자세 품질과 판정 보류

검토기는 사람·관절 품질 조건을 먼저 확인합니다. 예측이 없거나 품질 조건을
통과하지 못하면 7이나 9를 강제로 고르지 않고 판정 보류합니다.

취약 조건:

- 다중 인물
- 팔·몸통 가림
- 화면 밖으로 잘린 손목과 팔꿈치
- 매우 작은 사람
- 흐림·저조도·역광
- 비정상 카메라 각도
- 자세 인식기가 다른 사람의 관절을 섞은 경우

품질 PASS도 정답이나 구조 의도를 보증하지 않습니다. 사람이 오버레이를 확인해야
합니다.

## 시간축 한계

hard4는 TCN 구조를 사용하지만 이번 적응 학습과 50장 평가는 정지 관절을 48프레임
동안 반복한 입력입니다. 실제 움직임, 전환, 유지시간, 중립 구간과 같은 시간축
증거를 학습·평가한 것이 아닙니다.

정지사진 한 장으로 다음을 판단할 수 없습니다.

- 같은 사람이 7과 9를 순서대로 수행했는가
- 각 자세를 충분히 유지했는가
- 토큰 사이에 중립 동작이 있었는가
- 전체 `7→9→7→9`가 제한시간 안에 완성됐는가

## 실제 배포 전 필요한 검증

1. 동의받은 서로 다른 실제 참여자와 카메라의 독립 holdout
2. 실제 완전한 `7979` 연속영상
3. 전화·머리 만지기·손 흔들기 등 강한 음성 사례
4. 야간·역광·원거리·가림·다인 장면
5. event-level recall과 false candidates per camera-hour
6. 판정 보류율과 최소 사람 픽셀 크기
7. 처리 지연과 실제 운영 속도
8. 개인정보·보안·기관 검토
9. 자동 신고 없이 사람 검토만 수행하는 shadow pilot

## 인간 감독과 운영 권한

- 사람 검토 필수: `true`
- 외부 자동신고: `false`
- 자동 출동: `false`
- 공인 응급서비스: `false`
- 실제 CCTV 성능 주장: `false`

모델 결과는 원본 확인을 위한 후보·진단 정보로만 취급합니다.

## 모델 파일과 무결성

공개 모델:

```text
models/rescue79-hard4-portable-v1.pt
```

배포 모델은 특정 개발 PC의 절대경로에 의존하지 않는 portable 형식이어야 합니다.
공개 모델 SHA-256은 다음과 같으며 사용자는 실행 전 `Get-FileHash`로 확인하는
것을 권장합니다.

```text
603cf711a4ccd59119c63a207bb78f3399ce8860c6eed5d6692481f13ff2db0a
```

검토기는 지원 체크포인트를 `torch.load(weights_only=True)`로 읽고 등록된 스키마와
필요한 출처 해시를 확인합니다. 이는 안전성을 높이는 조치지만 출처가 불분명한
모델을 불러와도 된다는 보장은 아닙니다.

## 별도 자세 모델

RGB 관절 추출에는 다음 Torchvision 가중치를 사용합니다.

- 구조: `keypointrcnn_resnet50_fpn`
- 가중치: `KeypointRCNN_ResNet50_FPN_Weights.COCO_V1`
- 학습 데이터 표기: COCO train2017 person keypoints
- 프로젝트 hard4에 포함: 아니요
- 프로젝트 MIT로 재허가: 아니요

자세 가중치와 데이터 유래 권리는 [제3자 고지](THIRD_PARTY_NOTICES.md)를
참고하세요.

## 라이선스와 보증

프로젝트 소유자는 `models/rescue79-hard4-portable-v1.pt` 가중치를 저장소의 MIT
조건으로 공개합니다. 세부 범위는 [모델 라이선스](MODEL_LICENSE.md)를 따릅니다.

MIT 허가는 실제 CCTV 성능, 정확성, 안전성, 특정 목적 적합성, 구조 의도 판정 또는
기관 승인을 보증하지 않습니다.

## 권장 인용 문구

> Rescue79 hard4 portable v1은 생성형 정지영상 진단용 7·9 어댑터다. 사전 고정한
> 생성형 정지사진 50장에서는 D7 25/25, D9 25/25를 판별했으나 실제 CCTV 검출률,
> 오경보율과 완전한 7979 시간 순서 성능은 아직 측정하지 않았다.

## 변경관리

다음 사항이 바뀌면 새 모델 버전과 새 해시를 발급해야 합니다.

- 7·9 자세 정의
- 좌우 반전 정책
- 입력 관절·특징·프레임 수
- 자세 품질 조건
- 모델 가중치
- 학습·평가자료
- 판정 또는 점수 해석
- 외부 시스템 연결 범위
