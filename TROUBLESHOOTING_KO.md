# 문제 해결

문제가 생기면 먼저 실제 긴급상황이 아닌지 확인하세요. 실제 위험상황이라면 이
프로그램을 고치는 것보다 사람이 현장을 확인하고 공식 긴급 연락수단을 이용하는
것이 우선입니다.

## 먼저 확인할 다섯 가지

1. ZIP 안이 아니라 압축을 푼 폴더에서 실행했나요?
2. `START_RESCUE79.cmd`와 `models/rescue79-hard4-portable-v1.pt`가 있나요?
3. 검은색 실행 창을 닫지 않았나요?
4. 최초 설치와 첫 실행 때 인터넷이 연결돼 있나요?
5. 실제 CCTV가 아닌 개인정보 없는 예제로 먼저 시험했나요?

## 프로그램을 눌렀는데 검은색 창이 바로 닫혀요

파일 탐색기 더블클릭만으로는 오류를 읽기 어렵습니다. 프로그램 폴더에서 PowerShell을
열고 다음 명령을 실행하세요.

```powershell
.\START_RESCUE79.cmd
```

마지막 오류 문구를 확인합니다. 비밀번호, 사용자 이름, 실제 사진 경로 같은 정보는
지운 뒤 제보하세요.

주요 원인:

- uv가 설치되지 않음
- 프로젝트 환경이 준비되지 않음
- 모델 파일 누락
- ZIP 내부에서 실행함
- 보안 프로그램이 파일을 차단함
- 다른 프로그램이 이미 같은 로컬 주소를 사용함

## `uv`를 찾을 수 없다고 나와요

PowerShell에서 확인합니다.

```powershell
uv --version
```

명령을 찾을 수 없으면 담당자가 uv를 설치한 뒤 PowerShell을 새로 열어야 합니다.
공식 설치 안내:

<https://docs.astral.sh/uv/getting-started/installation/>

Windows Package Manager를 사용할 수 있는 환경에서는 다음 명령을 사용할 수
있습니다.

```powershell
winget install --id=astral-sh.uv -e
```

조직 PC에서는 임의 설치 전 전산 담당자에게 문의하세요.

## Python 또는 패키지 설치 오류가 나요

프로그램 폴더에서 다음 명령을 실행합니다.

```powershell
uv sync --frozen
```

확인할 사항:

- 인터넷 연결
- 디스크 여유 공간
- 조직의 프록시·방화벽 정책
- Python 3.12 다운로드 허용 여부
- 백신이 `.venv` 작성을 막고 있지 않은지
- `uv.lock`이 Release 원본과 같은지

`uv.lock`을 임의로 삭제하거나 의존성 버전을 임의로 올리면 같은 환경을 재현하기
어려워집니다.

환경이 손상된 것 같다면 기존 `.venv`를 즉시 삭제하지 말고 프로그램을 종료한 뒤
`.venv.old`처럼 이름을 바꾸어 보존하고 다시 `uv sync --frozen`을 실행하세요. 새
환경이 정상임을 확인한 뒤 이전 환경의 처리 여부를 결정하세요.

## 브라우저가 자동으로 열리지 않아요

검은색 실행 창이 계속 켜져 있는지 확인하세요. 창에 로컬 접속 주소가 표시되면 그
주소를 브라우저 주소창에 직접 입력합니다.

실행 창도 끝났다면 그 창의 오류부터 해결해야 합니다. 브라우저 새로고침만으로
서버가 다시 시작되지는 않습니다.

## 브라우저에는 나오지만 모델 상태가 준비되지 않아요

다음을 확인합니다.

- `models/rescue79-hard4-portable-v1.pt`가 존재함
- 파일명이 정확함
- 공개 모델 SHA-256과 현재 파일 해시가 같음
- 개발 PC의 절대경로에 의존하지 않는 portable 모델임
- 첫 실행의 COCO 자세 가중치 다운로드가 끝남

모델 해시 확인:

```powershell
Get-FileHash .\models\rescue79-hard4-portable-v1.pt -Algorithm SHA256
```

정상 SHA-256은 다음과 같습니다.

```text
603cf711a4ccd59119c63a207bb78f3399ce8860c6eed5d6692481f13ff2db0a
```

해시가 다르면 모델을 고치거나 다시 저장하지 말고 공식 Release를 다시
다운로드하세요.

## 모델 파일이 없다고 나와요

소스 ZIP을 받은 경우 대용량 모델이 빠졌을 수 있습니다. 공식 Release에서 Windows
배포 ZIP을 다시 받고 전체 압축을 풉니다.

다음 경로를 임의로 바꾸지 마세요.

```text
models\rescue79-hard4-portable-v1.pt
```

## 처음 실행할 때 다운로드 오류가 나요

RGB 사진의 관절을 찾는 COCO 자세 가중치는 hard4 모델과 별도입니다. 첫 실행 때
Torchvision 공식 다운로드 서버에 연결할 수 있어야 합니다.

확인할 사항:

- 인터넷 연결
- 조직 프록시와 TLS 검사 정책
- 다운로드 서버 차단 여부
- 사용자 캐시 폴더 쓰기 권한
- 디스크 여유 공간

임의의 블로그나 파일공유 사이트에서 가중치를 받지 마세요. 자세 가중치의 공식
파일 정보는 [제3자 고지](THIRD_PARTY_NOTICES.md)를 확인하세요.

## 모델 검토 시작 버튼이 회색이에요

다음 세 조건이 모두 필요합니다.

- 모델 준비 완료
- 기대값 7 또는 9 선택
- PNG, JPG/JPEG 또는 WEBP 이미지 선택

한 항목이 빠지면 버튼이 활성화되지 않습니다.

## 이미지 파일을 선택할 수 없어요

지원 형식:

- PNG
- JPG/JPEG
- WEBP

최대 파일 크기는 12 MiB입니다. 파일 확장자만 바꾼 가짜 이미지나 손상된 파일은
읽을 수 없습니다.

문서, PDF, 동영상과 HEIC 파일은 현재 정지사진 검토 입력이 아닙니다. 원본의 권리와
품질을 유지할 수 있는 승인된 방법으로 PNG 또는 JPG 시험자료를 별도로 준비하세요.

## 파일 크기 제한 오류가 나요

이미지가 12 MiB를 넘는지 확인합니다. 개인정보 없는 시험 이미지라면 승인된
이미지 도구로 크기를 줄인 복사본을 만드세요. 실제 증거 원본은 덮어쓰지 말고 별도
읽기 전용 보관정책을 따르세요.

## 판정이 너무 오래 걸려요

사람 관절 검출은 CPU에서 시간이 걸릴 수 있습니다.

- 결과가 나올 때까지 버튼을 반복해서 누르지 않습니다.
- 다른 무거운 프로그램을 닫습니다.
- 한 번에 한 장만 처리합니다.
- 매우 큰 이미지는 합리적인 크기의 시험 복사본을 사용합니다.
- 검은색 실행 창에 메모리 오류가 없는지 확인합니다.

현재 공개판은 실시간 CCTV 처리 성능을 보증하지 않습니다.

## 판정 보류가 자주 나와요

판정 보류는 안전한 정상 결과일 수 있습니다. 다음 조건의 사진으로 다시 시험하세요.

- 한 사람만 크게 보임
- 머리, 양어깨, 양팔과 골반이 보임
- 손목과 팔꿈치가 사진 밖으로 잘리지 않음
- 다른 사람이나 물체가 팔을 가리지 않음
- 충분히 밝고 선명함

판정 보류를 억지로 7 또는 9로 바꾸지 마세요.

## 여러 사람이 있는 사진에서 판정 보류가 나와요

검토기는 한 사람 조건을 중요하게 봅니다. 여러 사람의 관절을 섞으면 잘못된 자세가
될 수 있기 때문입니다.

연구용 시험에서 한 사람 영역을 별도로 준비하려면 자료 사용권한과 개인정보 보호를
확인하세요. 실제 관제 결정을 위해 문맥을 제거한 자르기 결과만 사용하면 안 됩니다.

## 관절선이 사람과 맞지 않아요

모델 예측보다 관절 입력이 먼저 잘못된 상황입니다.

- 결과를 성능 성공으로 기록하지 않습니다.
- 사진의 조명, 거리, 가림과 다중 인물을 기록합니다.
- 더 선명한 시험자료로 다시 확인합니다.
- 실제 구조 의도를 추측하지 않습니다.

관절 품질이 PASS여도 사람이 보기에는 잘못 연결될 수 있으므로 원본 비교가
필수입니다.

## 모델 점수가 높은데 틀렸어요

모델 점수는 보정된 정답 확률이 아닙니다. 익숙하지 않은 장면에서도 높은 점수가
나올 수 있습니다. 높은 점수를 이유로 오답을 정답으로 바꾸지 마세요.

자세한 해석은 [결과 읽는 법](RESULT_GUIDE_KO.md)을 참고하세요.

## 실행할 때 이미 사용 중이라는 오류가 나요

검토기가 이미 실행 중일 수 있습니다.

1. 열려 있는 검은색 실행 창을 찾습니다.
2. 기존 브라우저 탭에서 검토기가 열려 있는지 확인합니다.
3. 기존 실행 창에서 `Ctrl + C`로 정상 종료합니다.
4. 잠시 뒤 한 번만 다시 실행합니다.

관련 없는 프로세스를 강제로 종료하지 마세요.

## 브라우저만 닫았는데 프로그램이 계속 실행돼요

브라우저 탭과 로컬 서버는 별개입니다. 검은색 실행 창을 선택하고 `Ctrl + C`를
눌러 종료하세요.

## Windows SmartScreen 또는 백신 경고가 나와요

경고를 무조건 우회하지 마세요.

- 공식 GitHub·GitLab Release인지 확인
- 배포 파일 SHA-256 확인
- 서명 또는 게시자 정보 확인
- 조직 PC라면 전산 담당자 검토
- 출처를 확인할 수 없으면 실행 중단

공개 소스라는 이유만으로 내려받은 모든 실행 파일이 자동으로 안전해지는 것은
아닙니다.

## 완전히 오프라인에서 실행되지 않아요

다음 준비가 인터넷 연결 상태에서 완료됐는지 확인하세요.

- uv와 Python 3.12 환경 준비
- `uv sync --frozen`
- COCO 자세 가중치 다운로드
- 예제 이미지 최초 판별

오프라인 배포를 별도로 만들 때는 모든 제3자 바이너리와 가중치의 재배포 조건을
검토해야 합니다. 현재 프로젝트 MIT 라이선스가 COCO 자세 가중치를 재허가하지는
않습니다.

## 문제 제보 전에 모을 정보

- Windows 버전
- Release 이름 또는 커밋
- `uv --version` 결과
- 오류 문구
- 모델 SHA-256
- 입력 파일 형식과 크기
- 문제 재현 순서
- 개인정보를 제거한 화면 캡처

다음 정보는 올리지 마세요.

- 실제 CCTV 원본
- 얼굴, 이름, 주소와 차량번호
- 비밀번호, API 키, 내부 서버 주소
- 조직 내부 경로와 민감한 로그

안전한 자료로 같은 문제를 재현할 수 없다면 공개 이슈 대신 조직의 승인된 보안
연락수단을 사용하세요.
